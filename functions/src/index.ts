export { cleanupDeletedUser } from "./auth";
export {
  onFollowCreated,
  onFollowDeleted,
  onConnectionCreated,
  onConnectionUpdated,
} from "./social";

// Phase 5 adds: onMessageCreated (notifications.ts), collaborator/connection
// recommendations (recommendations.ts), portfolio + profile analytics
// aggregation (analytics.ts)
